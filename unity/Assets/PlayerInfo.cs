using UnityEngine;
using System.Collections;

public class PlayerInfo : MonoBehaviour {
	public string name = "";
	public string id = "";



/*	IEnumerator Start (string id) {

		string url = "localhost:7000/actions/userInteraction/pokeMe?accessToken="+Toolbox.Instance.authenticationToken+"&friendID="+id;

		WWW www = new WWW(url);
		yield return www;
		
		if (www.error == null)
		{
			Debug.Log("Poke Success");
		} else {
			Debug.LogError("Poke didn't work");
		}  
	}*/
}
